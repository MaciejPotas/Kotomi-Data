# Grammar

This directory contains reusable grammar data used by the Schema 1 project.

- `grammar_rules.xml` defines grammar roles, features, entities, and related grammar metadata.
- `contexts.xml` contains reusable context pools used by sentence generation.

Both files are loaded through `../quiz_project.xml`. Keep grammar-wide definitions here rather than placing them beside dictionaries or sentence patterns.
